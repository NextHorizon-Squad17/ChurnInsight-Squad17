name: 🛡️ Nexus Sanity Check

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  data-science-validation:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout do Código
        uses: actions/checkout@v4

      - name: ⚙️ Configurar Segredos Fakes
        # Cria um .env dummy apenas para o Docker não reclamar da falta do arquivo
        run: |
          cd data-science
          echo "GOOGLE_API_KEY=ci_test_dummy_key" > .env

      - name: 🐳 Build & Test (Via Docker)
        # Estratégia Enterprise: Constrói o container e roda os testes DENTRO dele.
        # Se o teste falhar, o docker run retorna erro e quebra o pipeline (como deve ser).
        run: |
          cd data-science
          
          echo "1. Construindo Imagem..."
          docker build -t churn-ci-image .
          
          echo "2. Executando Pytest dentro do Container..."
          docker run --rm churn-ci-image pytest -v